# Configuration minimaliste pour Platform.sh
applications:
  meditrack-laravel:
    type: php:8.2
    build:
      flavor: composer
    web:
      locations:
        "/":
          root: "public"
          passthru: "/index.php"
    hooks:
      build: |
        composer install --no-dev --optimize-autoloader
    disk: 2048